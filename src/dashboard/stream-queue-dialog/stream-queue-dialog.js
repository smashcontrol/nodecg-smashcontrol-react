import React from 'react';
import ReactDOM from 'react-dom/client';
import { StreamQueueDialog } from './StreamQueueDialog';

const root = ReactDOM.createRoot(document.querySelector('#app'));

root.render(
    <StreamQueueDialog />
);